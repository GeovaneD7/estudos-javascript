// isEven 

function isEven(numero) {
    let absolute = Math.abs(nummero);
    if ((absolute % 2) === 0 ) {
        return true;
    } else if ((absolute % 2) === 1) {
        return false;
    } else {

    }
    
}
