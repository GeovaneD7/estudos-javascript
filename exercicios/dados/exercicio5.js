// Crie uma variável com uma string 
// utilize métodos de manipulação de strings, como toUpperCase, toLowerCase, slice ou outros, para modificar a string original.
//  Exiba os resultados finais no console.

let nomeCompleto = `Geovane Dias de Araujo`;

console.log(nomeCompleto.toUpperCase());
console.log(nomeCompleto.toLowerCase());
console.log(nomeCompleto.slice(0, 15));
console.log(nomeCompleto.length);
